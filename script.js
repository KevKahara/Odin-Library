//Have an array where you will store books

const myLibrary = [];

//Constructor of books with each book will have a unique id

function Book(title, author, pages, isRead) {
    this.id = crypto.randomUUID();
    this.title = title;
    this.author = author;
    this.pages = pages;
    this.isRead = isRead;
}

//function that will create a book and then add the book to the array
function addBooktoLibrary() {
    addBooktoLibrary.push();
}

